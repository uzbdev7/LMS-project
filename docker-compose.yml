version: "3.8"

services:
  db:
    image: postgres:15
    container_name: LMS-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: lms_db
    ports:
      - "5433:5432" 
    volumes:
      - db_data:/var/lib/postgresql/data

  app:
    build: .
    container_name: LMS-backend
    restart: always
    depends_on:
      - db
    ports:
      - "7070:7070" 
    env_file:
      - .env 
    environment:

      DATABASE_URL: postgresql://neondb_owner:npg_AISNVb3jlye4@ep-royal-scene-agft7jbo-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=verify-full

    command: sh -c "npx prisma generate && npx prisma db push && pnpm run start:dev"

volumes:
  db_data: